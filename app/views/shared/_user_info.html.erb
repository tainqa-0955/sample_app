<%= link_to gravatar_for(current_user, size: Settings.image[:gravatar_user_size]), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("micropost.new.view_profile"), current_user %></span>
<span><%= t "micropost.new.total_micropost", count: current_user.microposts.count %></span>
